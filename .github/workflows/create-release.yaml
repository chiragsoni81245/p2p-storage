name: Create Release
run-name: Release (${{ github.event.inputs.version }})

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.0)'
        required: true
        type: string

jobs:
  build-and-release:
    name: Build & Release Go Binary
    runs-on: ubuntu-latest

    permissions:
      contents: write  # Required to push tags and create releases

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Important for tagging and version history

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build binaries
        run: |
          mkdir -p bin
          go mod tidy
          # Build for Linux
          GOOS=linux GOARCH=amd64 go build -o bin/p2p-storage-linux-amd64 ./cmd/node/main.go
          # Build for Windows
          GOOS=windows GOARCH=amd64 go build -o bin/p2p-storage-windows-amd64.exe ./cmd/node/main.go

      - name: Verify build
        run: |
          ls -lh bin/

      - name: Create Git tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag ${{ github.event.inputs.version }}
          git push origin ${{ github.event.inputs.version }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version }}
          name: ${{ github.event.inputs.version }}
          files: |
            bin/p2p-storage-linux-amd64
            bin/p2p-storage-windows-amd64.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}